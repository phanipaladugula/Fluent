# SQLite file. Docker Compose sets this to /data/lingo.db
DATABASE_PATH=./data/lingo.db

# Comma-separated browser origins allowed to call the API.
# On Railway include the frontend URL, e.g. https://fluent.rdrt.dev
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://fluent.rdrt.dev
