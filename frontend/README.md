# Fluent frontend

Next.js app for Fluent. Setup instructions live in the root [README](../README.md).

```bash
npm install
npm run dev
```

The UI talks to `/api/*`. Next.js rewrites those calls to the FastAPI server.
